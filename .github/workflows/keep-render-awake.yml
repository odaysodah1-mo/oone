name: Keep Render awake
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s --max-time 15 "https://oone-api-xxpp.onrender.com/api/healthz" | grep -q '"ok"' && echo "alive" || echo "check failed"
