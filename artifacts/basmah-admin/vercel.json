{
  "buildCommand": "cd ../.. && pnpm --filter @workspace/basmah-admin run build",
  "outputDirectory": "artifacts/basmah-admin/dist/public",
  "installCommand": "pnpm install --frozen-lockfile --ignore-scripts",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://oone-api-xxpp.onrender.com/api/$1" }
  ]
}
