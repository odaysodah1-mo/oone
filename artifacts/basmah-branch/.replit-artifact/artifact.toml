kind = "web"
previewPath = "/basmah-branch/"
title = "بصمة — لوحة الفرع"
version = "1.0.0"
id = "artifacts/basmah-branch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = ["/basmah-branch/"]
localPort = 8091

[services.development]
run = "pnpm --filter @workspace/basmah-branch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/basmah-branch", "run", "build" ]
publicDir = "artifacts/basmah-branch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8091"
BASE_PATH = "/basmah-branch/"
